<%= form_for(@lecturer) do |f| %>
  <% if @lecturer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lecturer.errors.count, "error") %> prohibited this lecturer from being saved:</h2>

      <ul>
      <% @lecturer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <div class="field">
      <tr>
	<td>
	  <%= f.label :title %>
	</td>
	<td>
	  <%= f.select :title, TITLES, :include_blank => true %>
	</td>
      </tr>
      <tr>
	<td>
	  <%= f.label :first %>
	</td>
	<td>
	  <%= f.text_field :first %>
	</td>
      </tr>
      <tr>
	<td>
	  <%= f.label :last %>
	</td>
	<td>
	  <%= f.text_field :last %>
	</td>
      </tr>
      <tr>
	<td>
	  <%= f.label :home_url %>
	</td>
	<td>
	  <%= f.text_field :home_url %>
	</td>
      </tr>
      <tr>
	<td>
	  <%= f.label :nuig_mail %>
	</td>
	<td>
	  <%= f.text_field :nuig_mail, :size => 16 %>
	  @nuigalway.ie
	</td>
      </tr>
      <tr>
	<td>
	  <%= f.label :discipline, "School" %>
	</td>
	<td>
	  <%= f.select :discipline, DISCIPLINES, :include_blank => true %>
	</td>
      </tr>
  </table>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
