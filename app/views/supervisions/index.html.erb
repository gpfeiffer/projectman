<h1>Listing supervisions</h1>

<table>
  <tr>
    <th>Lecturer</th>
    <th>Project</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @supervisions.each do |supervision| %>
  <tr>
    <td><%= supervision.lecturer_id %></td>
    <td><%= supervision.project_id %></td>
    <td><%= link_to 'Show', supervision %></td>
    <td><%= link_to 'Edit', edit_supervision_path(supervision) %></td>
    <td><%= link_to 'Destroy', supervision, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Supervision', new_supervision_path %>
